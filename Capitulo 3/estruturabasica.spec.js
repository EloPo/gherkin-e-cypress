describe('Nome do teste', () => {
    it('Nome do caso de teste', () => {
        // Preparação: Definir pré-condições, como visitar uma página, fazer login, etc.
        // Normalmente, essa etapa é realizada no bloco 'before' ou 'beforeEach'.
        // Ação: Executar a ação a ser testada, como clicar em um botão, preencher um formulário, etc.
        // Verificação: Verificar se o resultado da ação está de acordo com o esperado, usando asserções do Cypress.
        // Limpeza: Restaurar o estado do sistema para o estado original, se necessário.
        // Normalmente, essa etapa é realizada no bloco 'after' ou 'afterEach'.
    });
});
